- $Query:$给定 $n$ 组二元组 $(a_i,b_i)$ 从选出 $k$ 组,使得 $\frac{\sum a_i}{\sum b_i}$ 的值最大
- $Solution:$直接二分答案 $mid$ , 利用分式的值求出每个 $b_i$  所对应的理论 $a'_i$ , 那么对于每个 $i$ 都可以求出 $a_i$ $-$ $a'_i$ 我们记这个值为 $c_i$ , 那么 $c_i$ 事实上就是对于每个 $a_i$ 的结余,那么若 $\sum c_i \geq 0$ 则说明 $mid$ 合法,可以去尝试更大的值
